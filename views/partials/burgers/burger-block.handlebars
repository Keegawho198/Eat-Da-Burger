<li>
	{{name}}

	<button class="change-eaten" data-id="{{id}}" data-newsleep="{{eaten}}">
		{{#if eaten}}Eat this!{{else}}Not eaten!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
